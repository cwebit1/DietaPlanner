name: Apply DietaPlanner v22
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/scripts/apply_v22.py'
jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python .github/scripts/apply_v22.py
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git diff --cached --quiet || git commit -m "Make protein selectors identical to carbohydrate selectors v22 [skip ci]"
          git push
