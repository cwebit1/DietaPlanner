name: Apply DietaPlanner v30
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/scripts/apply_v30.py'
      - '.github/triggers/v30.txt'
jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Apply v30
        run: python .github/scripts/apply_v30.py
      - name: Check JavaScript
        run: |
          node --check motor-v9-2.js
          node --check motor-v9-3.js
          python - <<'PY'
          import re, subprocess, pathlib
          s=pathlib.Path('index.html').read_text(encoding='utf-8')
          for i,code in enumerate(re.findall(r'<script(?:\s[^>]*)?>(.*?)</script>',s,re.S|re.I)):
              p=pathlib.Path(f'/tmp/inline_{i}.js')
              p.write_text(code,encoding='utf-8')
              subprocess.run(['node','--check',str(p)],check=True)
          PY
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html motor-v9-2.js motor-v9-3.js
          git diff --cached --quiet || git commit -m "Schedule recurring vegetables and cap exclusions v30 [skip ci]"
          git push
